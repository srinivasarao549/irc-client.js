var IRC    = require(path.join(process.cwd(), 'lib', 'irc', 'client')),
    Client = IRC.Client;

describe("An IRC client", function() {
  beforeEach(function() {
    
  });

  it("should ...", function() {
    // ...
  });
})